services:
  - type: web
    name: fitp-web-monitor
    env: python
    runtime: python3.12
    plan: free
    buildCommand: pip install -r requirements.txt && playwright install chromium
    startCommand: python src/main.py
    envVars:
      - key: NODE_VERSION
        value: 20